/*





function createCar(plate:string,brand:string,color:string,wheels: any){
    let car = new Car(plate.value, color.value, brand.value);
    car.addWheel(new Wheel(wheelDiameter.value,wheelBrand.value));
    document.body.innerText="CAR: PLATE: " + car.plate 
    + " COLOR: " +car.color + " BRAND: " + brand 
    + " WHEELS: " + JSON.stringify(car.wheels);
}

*/
